/**
 * Type Inference: Ability of TS to automatically infer the type based on the value specified
 * to the variable or constant
 */

// infers the type as number
const score = 2332;

// infers the type as string
const playerName = 'dddd';
